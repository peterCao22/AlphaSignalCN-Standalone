
# AlphaSignalCN-Standalone minimal runtime dependencies
#
# Notes:
# - This list is inferred from imports inside the standalone directory (predict_stock.py / scripts / stockainews)
# - Install Playwright browsers after pip install:
#     python -m playwright install

# Core data stack
pandas>=1.5,<3.0
numpy>=1.23,<3.0

# Model loading / ML
joblib>=1.2,<2.0
scikit-learn>=1.2,<2.0
lightgbm>=4.0,<5.0
xgboost>=2.0,<3.0
tqdm>=4.66,<5.0

# HTTP / scraping
httpx>=0.26,<1.0
beautifulsoup4>=4.12,<5.0
playwright>=1.40,<2.0

# Config / logging
pydantic>=2.6,<3.0
pydantic-settings>=2.0,<3.0
python-dotenv>=1.0,<2.0
loguru>=0.7,<1.0

